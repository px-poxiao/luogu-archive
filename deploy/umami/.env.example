# Umami 运行环境配置。复制为 .env 后填写，不要提交真实 .env。

UMAMI_DB_NAME=umami
UMAMI_DB_USER=umami
UMAMI_DB_PASSWORD=请替换为随机强密码
UMAMI_APP_SECRET=请替换为至少32位随机字符串
UMAMI_LOCAL_PORT=3001

# 建议上线前固定为具体版本；留空时使用官方 PostgreSQL 镜像标签。
UMAMI_IMAGE=docker.umami.is/umami-software/umami:postgresql-latest
